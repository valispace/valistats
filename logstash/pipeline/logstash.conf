input {
	http {
		port => 8080
		user => "valispace"
		password => "${LOGS_PASSWORD}" 
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "${ELASTIC_PASSWORD}"
		index => "valispace-logs-http"
	}
}
